<!--
  Tool Name: Smart Connect Settings Window
  Description: Lets the user choose connection angle for Smart Connect.
  Author: Ajmal P.S.
  Version: 1.0.0
  Last Updated: 2026-03-25
  Revit Version: 2020
  Dependencies: AJTools.Models, AJTools.Services.SmartConnect
-->
<Window x:Class="AJTools.UI.SmartConnectWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Smart Connect"
        Width="460"
        MinWidth="460"
        MaxWidth="460"
        ResizeMode="NoResize"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        WindowStyle="ToolWindow"
        Background="{DynamicResource BackgroundDark}">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="ModernStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="12">
        <Border Background="{StaticResource CardBackground}"
                CornerRadius="8"
                Padding="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0"
                            Margin="0,0,0,10">
                    <TextBlock Text="Smart Connect"
                               FontSize="16"
                               FontWeight="Bold"
                               Foreground="{StaticResource TextPrimary}"/>
                    <TextBlock Text="Single Elbow mode | Continuous pick until Esc"
                               Foreground="{StaticResource TextSecondary}"
                               FontSize="12"
                               Margin="0,2,0,0"/>
                </StackPanel>

                <Border Grid.Row="1"
                        Background="{StaticResource BackgroundMedium}"
                        CornerRadius="6"
                        Padding="10"
                        Margin="0,0,0,10">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Row="0"
                                   Grid.ColumnSpan="2"
                                   Text="Angle"
                                   Foreground="{StaticResource TextPrimary}"
                                   FontWeight="SemiBold"
                                   Margin="0,0,0,6"/>

                        <UniformGrid Grid.Row="1"
                                     Grid.ColumnSpan="2"
                                     Columns="2"
                                     Margin="0,0,0,8">
                            <RadioButton x:Name="Angle45Radio"
                                         Content="45&#176;"
                                         GroupName="AnglePreset"
                                         Style="{StaticResource ModernRadioButton}"
                                         Checked="OnPresetAngleChecked"
                                         HorizontalAlignment="Left"/>
                            <RadioButton x:Name="Angle90Radio"
                                         Content="90&#176;"
                                         GroupName="AnglePreset"
                                         Style="{StaticResource ModernRadioButton}"
                                         Checked="OnPresetAngleChecked"
                                         HorizontalAlignment="Left"/>
                        </UniformGrid>

                        <TextBlock Grid.Row="2"
                                   Grid.ColumnSpan="2"
                                   Text="Custom angle (5 to 175)"
                                   Foreground="{StaticResource TextSecondary}"
                                   FontSize="12"
                                   Margin="0,0,0,4"/>

                        <TextBox x:Name="CustomAngleBox"
                                 Grid.Row="3"
                                 Grid.Column="0"
                                 Height="28"
                                 Style="{StaticResource ModernTextBox}"
                                 VerticalContentAlignment="Center"/>

                        <Button x:Name="AddAngleButton"
                                Grid.Row="3"
                                Grid.Column="1"
                                Content="Add Angle"
                                Style="{StaticResource ModernButton}"
                                Height="30"
                                MinWidth="96"
                                Margin="8,0,0,0"
                                Padding="12,4"
                                FontSize="12"
                                FontWeight="SemiBold"
                                Click="OnAddCustomAngleClick"/>

                        <ListBox x:Name="CustomAnglesList"
                                 Grid.Row="4"
                                 Grid.ColumnSpan="2"
                                 Height="96"
                                 Margin="0,8,0,0"
                                 Style="{StaticResource ModernListBox}"
                                 ItemContainerStyle="{StaticResource ModernListBoxItem}"
                                 SelectionMode="Single"
                                 SelectionChanged="OnCustomAnglesSelectionChanged"/>
                    </Grid>
                </Border>

                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock x:Name="ErrorText"
                               Grid.Column="0"
                               VerticalAlignment="Center"
                               Foreground="Crimson"
                               TextWrapping="Wrap"
                               Margin="0,0,10,0"/>

                    <Button Grid.Column="1"
                            Content="OK"
                            Style="{StaticResource ModernButton}"
                            Height="30"
                            MinWidth="88"
                            Margin="0,0,8,0"
                            Click="OnOkClick"/>

                    <Button Grid.Column="2"
                            Content="Cancel"
                            Style="{StaticResource SecondaryButton}"
                            Height="30"
                            MinWidth="88"
                            Click="OnCancelClick"/>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>
