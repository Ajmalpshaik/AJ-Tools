<!--
  Tool Name: Create Tags - Settings
  Description: Category enable/disable and minimum tag length for Create Tags.
  Author: Ajmal P.S.
  Version: 1.0.0
  Last Updated: 2026-07-28
  Revit Version: 2020 - latest
  Dependencies: AJTools.UI.ModernStyles
-->
<Window x:Class="AJTools.UI.CreateTags.CreateTagsSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create Tags Settings - AJ Tools"
        Width="640"
        Height="620"
        MinWidth="560"
        MinHeight="560"
        ResizeMode="CanResize"
        SizeToContent="Manual"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BackgroundDark}"
        Foreground="{DynamicResource TextPrimary}"
        FontFamily="Segoe UI">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../ModernStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <Style x:Key="CreateTagsGridHeaderStyle" TargetType="DataGridColumnHeader">
                <Setter Property="Background" Value="{StaticResource BackgroundLight}"/>
                <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                <Setter Property="BorderBrush" Value="{StaticResource BorderSoft}"/>
                <Setter Property="BorderThickness" Value="0,0,1,1"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Padding" Value="8,6"/>
                <Setter Property="HorizontalContentAlignment" Value="Left"/>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <DockPanel>
        <!-- Footer -->
        <TextBlock DockPanel.Dock="Bottom"
                   Text="Created &amp; All Rights Reserved @ Ajmal P.S."
                   Foreground="{StaticResource TextDisabled}"
                   FontSize="11"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,8"/>

        <Grid Margin="10">
            <Border Style="{StaticResource PanelCard}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Header -->
                    <StackPanel Grid.Row="0" Margin="0,0,0,10">
                        <TextBlock Text="Create Tags Settings"
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource TextPrimary}"/>
                        <TextBlock Text="Tick the categories you can pick from with Create Tags. An element is skipped automatically if it already has a tag in the view, if it's a vertical run, or if it's shorter than the minimum length below."
                                   FontSize="12"
                                   Foreground="{StaticResource TextSecondary}"
                                   TextWrapping="Wrap"
                                   Margin="0,4,0,0"/>
                    </StackPanel>

                    <!-- Quick actions -->
                    <StackPanel Grid.Row="1"
                                Orientation="Horizontal"
                                Margin="0,0,0,8">
                        <Button Content="Tag all"
                                Style="{StaticResource CompactSecondaryButton}"
                                MinWidth="80"
                                Margin="0,0,6,0"
                                Click="OnTagAll"
                                ToolTip="Tick every category."/>
                        <Button Content="Tag none"
                                Style="{StaticResource CompactSecondaryButton}"
                                MinWidth="80"
                                Click="OnTagNone"
                                ToolTip="Untick every category."/>
                    </StackPanel>

                    <!-- Category grid -->
                    <DataGrid x:Name="CategoryGrid"
                              Grid.Row="2"
                              Style="{StaticResource ModernDataGrid}"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              CanUserResizeRows="False"
                              CanUserReorderColumns="False"
                              CanUserSortColumns="False"
                              IsReadOnly="False"
                              SelectionMode="Single"
                              SelectionUnit="FullRow"
                              GridLinesVisibility="Horizontal"
                              HeadersVisibility="Column"
                              AlternationCount="2"
                              RowBackground="{StaticResource CardBackground}"
                              AlternatingRowBackground="{StaticResource BackgroundMedium}"
                              RowHeight="34"
                              ColumnHeaderStyle="{StaticResource CreateTagsGridHeaderStyle}">
                        <DataGrid.Resources>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                            </Style>
                        </DataGrid.Resources>
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="Tag?" Width="60">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"
                                                  Style="{StaticResource ModernCheckBox}"
                                                  IsChecked="{Binding Enabled, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="Element Type"
                                                Binding="{Binding CategoryLabel, Mode=OneWay}"
                                                IsReadOnly="True"
                                                Width="*"/>
                            <DataGridTextColumn Header="In Model"
                                                Binding="{Binding CountInModel, Mode=OneWay}"
                                                IsReadOnly="True"
                                                Width="90"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <!-- Minimum length input -->
                    <StackPanel Grid.Row="3" Margin="0,12,0,0">
                        <TextBlock Text="Minimum length to tag"
                                   FontWeight="SemiBold"
                                   FontSize="12"
                                   Foreground="{StaticResource TextPrimary}"
                                   Margin="0,0,0,5"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="140"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="MinLengthBox"
                                     Grid.Column="0"
                                     Style="{StaticResource ModernTextBox}"
                                     MaxLength="12"
                                     TextChanged="OnMinLengthChanged"
                                     ToolTip="Ducts, pipes, and cable trays shorter than this are skipped. Doesn't apply to equipment or accessories."/>
                            <TextBlock Grid.Column="1"
                                       Text="mm"
                                       FontSize="12"
                                       Margin="8,0,0,0"
                                       VerticalAlignment="Center"
                                       Foreground="{StaticResource TextSecondary}"/>
                        </Grid>

                        <CheckBox x:Name="SkipVerticalBox"
                                  Content="Skip vertical ducts, pipes and cable trays"
                                  Style="{StaticResource ModernCheckBox}"
                                  Margin="0,14,0,0"
                                  ToolTip="A run going straight up or down is skipped instead of tagged."/>
                        <TextBlock Text="Also used by Smart MEP Tags and Stack Tags, so all three treat a vertical run the same way."
                                   FontSize="11"
                                   Foreground="{StaticResource TextSecondary}"
                                   TextWrapping="Wrap"
                                   Margin="0,3,0,0"/>
                    </StackPanel>

                    <!-- Inline validation message -->
                    <TextBlock x:Name="ErrorText"
                               Grid.Row="4"
                               FontSize="12"
                               Foreground="{StaticResource AccentDanger}"
                               TextWrapping="Wrap"
                               Margin="0,10,0,0"/>

                    <!-- Buttons -->
                    <Grid Grid.Row="5" Margin="0,12,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Button Grid.Column="1"
                                x:Name="SaveButton"
                                Content="Save"
                                Style="{StaticResource ModernButton}"
                                MinWidth="104"
                                Margin="0,0,8,0"
                                IsDefault="True"
                                Click="OnSave"/>

                        <Button Grid.Column="2"
                                Content="Cancel"
                                Style="{StaticResource SecondaryButton}"
                                MinWidth="104"
                                IsCancel="True"
                                Click="OnCancel"/>
                    </Grid>
                </Grid>
            </Border>
        </Grid>
    </DockPanel>
</Window>
