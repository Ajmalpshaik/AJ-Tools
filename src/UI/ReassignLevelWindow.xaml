<!--
  Tool Name: Reassign Reference Level UI
  Description: WPF dialog for choosing source and target levels.
  Author: Ajmal P.S.
  Version: 1.0.0
  Last Updated: 2026-07-02
  Revit Version: 2020
-->
<Window x:Class="AJTools.UI.ReassignLevelWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Reassign Level"
        Width="560"
        MinWidth="520"
        SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BackgroundDark}"
        Foreground="{DynamicResource TextPrimary}">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="ModernStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Style="{StaticResource PanelCard}"
                Margin="0,0,0,8">
            <StackPanel>
                <TextBlock Text="Reassign Reference Level"
                           FontSize="18"
                           FontWeight="Bold"
                           Foreground="{StaticResource TextPrimary}"/>
                <TextBlock Text="Select the source and target levels for supported project elements."
                           Foreground="{StaticResource TextSecondary}"
                           TextWrapping="Wrap"
                           Margin="0,4,0,0"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1"
                Style="{StaticResource PanelCard}"
                Margin="0,0,0,8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Text="FROM Level"
                           Foreground="{StaticResource AccentOrange}"
                           FontWeight="SemiBold"
                           Margin="0,0,0,4"/>

                <ComboBox x:Name="FromLevelComboBox"
                          Grid.Row="1"
                          DisplayMemberPath="Label"
                          SelectionChanged="OnLevelSelectionChanged"/>

                <TextBlock Grid.Row="2"
                           Text="TO Level"
                           Foreground="{StaticResource AccentGreen}"
                           FontWeight="SemiBold"
                           Margin="0,12,0,4"/>

                <ComboBox x:Name="ToLevelComboBox"
                          Grid.Row="3"
                          DisplayMemberPath="Label"
                          SelectionChanged="OnLevelSelectionChanged"/>

                <TextBlock x:Name="ChangeSummaryText"
                           Grid.Row="4"
                           Foreground="{StaticResource TextSecondary}"
                           TextWrapping="Wrap"
                           Margin="0,12,0,0"/>
            </Grid>
        </Border>

        <Grid Grid.Row="2"
              Margin="0,6,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="StatusText"
                       Grid.Column="0"
                       VerticalAlignment="Center"
                       Foreground="{StaticResource TextSecondary}"
                       TextWrapping="Wrap"
                       Margin="0,0,12,0"/>

            <Button x:Name="ReassignButton"
                    Grid.Column="1"
                    Content="Reassign Elements"
                    Style="{StaticResource ModernButton}"
                    IsDefault="True"
                    MinWidth="142"
                    Margin="0,0,8,0"
                    Click="OnReassignClick"/>

            <Button Grid.Column="2"
                    Content="Cancel"
                    Style="{StaticResource SecondaryButton}"
                    IsCancel="True"
                    MinWidth="96"
                    Click="OnCancelClick"/>
        </Grid>
    </Grid>
</Window>
