<!--
==================================================
Tool Name    : Apply Graphics
Purpose      : Defines the WPF Apply Graphics settings window layout.
Author       : Ajmal P.S.
Company      : AJ Tools
Version      : 1.4.5
Created      : 2026-03-30
Last Updated : 2026-05-10
Target       : Revit 2020
Framework    : .NET Framework 4.7.2
Platform     : C# Revit Add-in
Dependencies : Autodesk Revit API, WPF
Input        : User graphics settings selections, apply mode choice, and selected categories.
Output       : Structured settings window for the combined Apply Graphics workflow.
Notes        : Normal success is silent; validation and critical errors are reported to the user.
Changelog    : v1.4.5 - Reduced default window size and restored native close/resize behavior for smaller screens.
License      : All Rights Reserved
Repo         : AJ-Tools
==================================================
-->

<Window x:Class="AJTools.UI.GraphicsTools.GraphicsOverrideWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Graphics Settings Manager"
        Width="620"
        Height="720"
        MinWidth="580"
        MinHeight="560"
        WindowStartupLocation="CenterScreen"
        WindowStyle="SingleBorderWindow"
        ResizeMode="CanResizeWithGrip"
        Background="#FF1E1E1E"
        Foreground="#FFFFFFFF"
        FontFamily="Segoe UI"
        FontSize="12"
        TextOptions.TextFormattingMode="Display"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True">
    <Window.Resources>
        <SolidColorBrush x:Key="WindowBackgroundBrush" Color="#FF1E1E1E"/>
        <SolidColorBrush x:Key="ShellBrush" Color="#FF2D2D30"/>
        <SolidColorBrush x:Key="TitleBarBrush" Color="#FF3E3E42"/>
        <SolidColorBrush x:Key="PanelBrush" Color="#FF252526"/>
        <SolidColorBrush x:Key="FieldBrush" Color="#FF252526"/>
        <SolidColorBrush x:Key="InnerPanelBrush" Color="#FF1E1E1E"/>
        <SolidColorBrush x:Key="BorderBrushSoft" Color="#FF3E3E42"/>
        <SolidColorBrush x:Key="BorderBrushStrong" Color="#FF454545"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#FF007ACC"/>
        <SolidColorBrush x:Key="AccentHoverBrush" Color="#FF1C97EA"/>
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="#FFFFFFFF"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#FFD0D0D0"/>
        <SolidColorBrush x:Key="TextMutedBrush" Color="#FF8D8D8D"/>
        <SolidColorBrush x:Key="TextDisabledBrush" Color="#FF686868"/>
        <SolidColorBrush x:Key="DangerBrush" Color="#FF5B1C1C"/>
        <SolidColorBrush x:Key="DangerBorderBrush" Color="#FF7A2828"/>
        <SolidColorBrush x:Key="DangerHoverBrush" Color="#FF8B2B2B"/>

        <Style TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <Style x:Key="SectionHeaderTextStyle" TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="TextOptions.TextFormattingMode" Value="Display"/>
        </Style>

        <Style x:Key="RowLabelTextStyle" TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="{StaticResource TextMutedBrush}"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="ButtonBaseStyle" TargetType="{x:Type Button}">
            <Setter Property="MinHeight" Value="28"/>
            <Setter Property="Padding" Value="12,4"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Opacity" Value="0.96"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Opacity" Value="0.82"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Root" Property="Opacity" Value="0.45"/>
                                <Setter Property="Foreground" Value="{StaticResource TextDisabledBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SmallSecondaryButtonStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource ButtonBaseStyle}">
            <Setter Property="MinWidth" Value="44"/>
            <Setter Property="MinHeight" Value="28"/>
            <Setter Property="Background" Value="{StaticResource TitleBarBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushStrong}"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="Padding" Value="10,3"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource BorderBrushStrong}"/>
                    <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SmallPrimaryButtonStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource ButtonBaseStyle}">
            <Setter Property="MinWidth" Value="48"/>
            <Setter Property="MinHeight" Value="28"/>
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="Padding" Value="10,3"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource AccentHoverBrush}"/>
                    <Setter Property="BorderBrush" Value="{StaticResource AccentHoverBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="FooterPrimaryButtonStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource ButtonBaseStyle}">
            <Setter Property="MinWidth" Value="108"/>
            <Setter Property="MinHeight" Value="32"/>
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="Padding" Value="16,7"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource AccentHoverBrush}"/>
                    <Setter Property="BorderBrush" Value="{StaticResource AccentHoverBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="FooterSecondaryButtonStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource ButtonBaseStyle}">
            <Setter Property="MinWidth" Value="88"/>
            <Setter Property="MinHeight" Value="30"/>
            <Setter Property="Background" Value="{StaticResource TitleBarBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushStrong}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="Padding" Value="16,6"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource BorderBrushStrong}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="FooterDangerButtonStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource ButtonBaseStyle}">
            <Setter Property="MinWidth" Value="96"/>
            <Setter Property="MinHeight" Value="32"/>
            <Setter Property="Background" Value="{StaticResource DangerBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource DangerBorderBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="Padding" Value="16,7"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource DangerHoverBrush}"/>
                    <Setter Property="BorderBrush" Value="{StaticResource DangerHoverBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ColorSwatchButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Width" Value="18"/>
            <Setter Property="Height" Value="18"/>
            <Setter Property="Margin" Value="0,0,6,0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushStrong}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="2"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="BorderBrush" Value="{StaticResource TextPrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Opacity" Value="0.80"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type TextBox}">
            <Setter Property="Height" Value="28"/>
            <Setter Property="Background" Value="{StaticResource FieldBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushSoft}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CaretBrush" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Padding" Value="28,4,8,4"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="10"/>
            <Style.Triggers>
                <Trigger Property="IsKeyboardFocused" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <ControlTemplate x:Key="ComboToggleTemplate" TargetType="{x:Type ToggleButton}">
            <Border x:Name="Root"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="3">
                <Path x:Name="Arrow"
                      Data="M 0 0 L 5 5 L 10 0 Z"
                      Fill="{StaticResource TextSecondaryBrush}"
                      Width="10"
                      Height="5"
                      HorizontalAlignment="Right"
                      VerticalAlignment="Center"
                      Margin="0,0,9,0"
                      Stretch="Uniform"/>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="Root" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                </Trigger>
                <Trigger Property="IsChecked" Value="True">
                    <Setter TargetName="Root" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                    <Setter TargetName="Arrow" Property="Fill" Value="{StaticResource AccentBrush}"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <Style TargetType="{x:Type ComboBox}">
            <Setter Property="Height" Value="28"/>
            <Setter Property="MinHeight" Value="28"/>
            <Setter Property="Background" Value="{StaticResource FieldBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushSoft}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,3"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBox}">
                        <Grid SnapsToDevicePixels="True">
                            <ToggleButton x:Name="ToggleButton"
                                          Focusable="False"
                                          ClickMode="Press"
                                          Template="{StaticResource ComboToggleTemplate}"
                                          Background="{TemplateBinding Background}"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          BorderThickness="{TemplateBinding BorderThickness}"
                                          IsChecked="{Binding Path=IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"/>
                            <ContentPresenter x:Name="ContentSite"
                                              Margin="9,0,28,0"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"
                                              TextElement.Foreground="{TemplateBinding Foreground}"
                                              IsHitTestVisible="False"/>
                            <TextBox x:Name="PART_EditableTextBox"
                                     Margin="9,0,28,0"
                                     Background="Transparent"
                                     BorderThickness="0"
                                     Foreground="{TemplateBinding Foreground}"
                                     CaretBrush="{StaticResource AccentBrush}"
                                     Visibility="Hidden"
                                     Focusable="True"
                                     IsReadOnly="{TemplateBinding IsReadOnly}"/>
                            <Popup x:Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Fade">
                                <Border x:Name="PopupBorder"
                                        Background="{StaticResource FieldBrush}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="1"
                                        MinWidth="{TemplateBinding ActualWidth}">
                                    <ScrollViewer Margin="2"
                                                  SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True"
                                                    KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEditable" Value="True">
                                <Setter TargetName="ContentSite" Property="Visibility" Value="Hidden"/>
                                <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.55"/>
                    <Setter Property="Foreground" Value="{StaticResource TextDisabledBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="{x:Type ComboBoxItem}">
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{StaticResource TitleBarBrush}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{StaticResource AccentBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CategoryListBoxStyle" TargetType="{x:Type ListBox}">
            <Setter Property="Background" Value="{StaticResource FieldBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushSoft}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="3"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
        </Style>

        <Style x:Key="CategoryListBoxItemStyle" TargetType="{x:Type ListBoxItem}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListBoxItem}">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                CornerRadius="3">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{StaticResource TitleBarBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CategoryCheckBoxStyle" TargetType="{x:Type CheckBox}">
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type CheckBox}">
                        <Grid Background="Transparent">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="24"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border x:Name="Box"
                                    Width="14"
                                    Height="14"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Background="Transparent"
                                    BorderBrush="#FF707070"
                                    BorderThickness="1"
                                    CornerRadius="2">
                                <Path x:Name="CheckMark"
                                      Data="M 2 7 L 5 10 L 11 3"
                                      Stroke="{StaticResource TextPrimaryBrush}"
                                      StrokeThickness="2"
                                      StrokeStartLineCap="Square"
                                      StrokeEndLineCap="Square"
                                      Visibility="Collapsed"/>
                            </Border>
                            <ContentPresenter Grid.Column="1"
                                              Margin="4,0,0,0"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"
                                              TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Box" Property="Background" Value="{StaticResource AccentBrush}"/>
                                <Setter TargetName="Box" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="False">
                                <Setter Property="Foreground" Value="{StaticResource TextMutedBrush}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CutLinkCheckBoxStyle" TargetType="{x:Type CheckBox}">
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type CheckBox}">
                        <Grid Background="Transparent">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="25"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border x:Name="Box"
                                    Width="16"
                                    Height="16"
                                    VerticalAlignment="Center"
                                    BorderBrush="#FF707070"
                                    BorderThickness="1"
                                    Background="Transparent"
                                    CornerRadius="2">
                                <Path x:Name="CheckMark"
                                      Data="M 3 8 L 7 12 L 13 4"
                                      Stroke="{StaticResource TextPrimaryBrush}"
                                      StrokeThickness="2"
                                      Visibility="Collapsed"/>
                            </Border>
                            <ContentPresenter Grid.Column="1"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"
                                              TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Box" Property="Background" Value="{StaticResource AccentBrush}"/>
                                <Setter TargetName="Box" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ToggleSwitchStyle" TargetType="{x:Type CheckBox}">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type CheckBox}">
                        <Grid Width="40" Height="20">
                            <Border x:Name="Track"
                                    Background="#FF666666"
                                    CornerRadius="10"/>
                            <Ellipse x:Name="Thumb"
                                     Width="12"
                                     Height="12"
                                     Fill="{StaticResource TextPrimaryBrush}"
                                     HorizontalAlignment="Left"
                                     Margin="4,0,0,0"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Track" Property="Background" Value="{StaticResource AccentBrush}"/>
                                <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right"/>
                                <Setter TargetName="Thumb" Property="Margin" Value="0,0,4,0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type TabControl}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TabControl}">
                        <Grid KeyboardNavigation.TabNavigation="Local">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Border Grid.Row="0"
                                    BorderBrush="{StaticResource BorderBrushSoft}"
                                    BorderThickness="0,0,0,1">
                                <TabPanel x:Name="HeaderPanel"
                                          IsItemsHost="True"
                                          KeyboardNavigation.TabIndex="1"
                                          Background="Transparent"/>
                            </Border>
                            <ContentPresenter x:Name="PART_SelectedContentHost"
                                              Grid.Row="1"
                                              ContentSource="SelectedContent"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type TabItem}">
            <Setter Property="Foreground" Value="{StaticResource TextMutedBrush}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="24,10"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TabItem}">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="Transparent"
                                BorderThickness="0,0,0,2"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter x:Name="Content"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              ContentSource="Header"
                                              TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{StaticResource ShellBrush}"/>
                                <Setter TargetName="Root" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="{StaticResource ShellBrush}"
            BorderBrush="{StaticResource BorderBrushSoft}"
            BorderThickness="0"
            CornerRadius="0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <ScrollViewer Grid.Row="0"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled"
                          Background="{StaticResource WindowBackgroundBrush}">
                <Border Margin="12"
                        Padding="16"
                        Background="{StaticResource PanelBrush}"
                        BorderBrush="{StaticResource BorderBrushSoft}"
                        BorderThickness="1"
                        CornerRadius="4">
                    <StackPanel>
                        <TextBlock Text="CATEGORY SELECTION SOURCE"
                                   Foreground="{StaticResource AccentBrush}"
                                   FontSize="11"
                                   FontWeight="Bold"
                                   Margin="0,0,0,14"/>

                        <Border Background="{StaticResource InnerPanelBrush}"
                                BorderBrush="{StaticResource BorderBrushSoft}"
                                BorderThickness="1"
                                CornerRadius="4"
                                Padding="14"
                                Margin="0,0,0,18">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid Grid.Row="0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <Grid Margin="0,0,12,0">
                                        <TextBox x:Name="CategorySearchBox"
                                                 TextChanged="OnCategorySearchChanged"
                                                 GotKeyboardFocus="OnCategorySearchFocusChanged"
                                                 LostKeyboardFocus="OnCategorySearchFocusChanged"/>
                                        <Grid Width="12"
                                              Height="12"
                                              HorizontalAlignment="Left"
                                              VerticalAlignment="Center"
                                              Margin="10,0,0,0"
                                              IsHitTestVisible="False">
                                            <Ellipse Width="8"
                                                     Height="8"
                                                     HorizontalAlignment="Left"
                                                     VerticalAlignment="Top"
                                                     Stroke="{StaticResource TextDisabledBrush}"
                                                     StrokeThickness="1.4"/>
                                            <Line X1="7"
                                                  Y1="7"
                                                  X2="12"
                                                  Y2="12"
                                                  Stroke="{StaticResource TextDisabledBrush}"
                                                  StrokeThickness="1.4"
                                                  StrokeStartLineCap="Round"
                                                  StrokeEndLineCap="Round"/>
                                        </Grid>
                                        <TextBlock x:Name="CategorySearchPlaceholderText"
                                                   Text="Search Categories..."
                                                   Foreground="{StaticResource TextDisabledBrush}"
                                                   FontSize="10"
                                                   VerticalAlignment="Center"
                                                   Margin="29,0,0,0"
                                                   IsHitTestVisible="False"/>
                                    </Grid>

                                    <Button Grid.Column="1"
                                            Content="ALL"
                                            Style="{StaticResource SmallSecondaryButtonStyle}"
                                            Margin="0,0,8,0"
                                            Click="OnSelectAllCategories"/>
                                    <Button Grid.Column="2"
                                            Content="NONE"
                                            Style="{StaticResource SmallSecondaryButtonStyle}"
                                            Click="OnClearCategories"/>
                                </Grid>

                <ListBox x:Name="CategoryListBox"
                                         Grid.Row="1"
                                         Height="96"
                                         Margin="0,12,0,0"
                                         Style="{StaticResource CategoryListBoxStyle}"
                                         ItemContainerStyle="{StaticResource CategoryListBoxItemStyle}">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                      Content="{Binding DisplayName}"
                                                      Style="{StaticResource CategoryCheckBoxStyle}"/>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                            </Grid>
                        </Border>

                        <TabControl x:Name="GraphicsTabControl" Margin="0,2,0,0">
                            <TabItem Header="PROJECTION / SURFACE">
                                <StackPanel Margin="2,14,2,0" MinHeight="430">
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,14">
                                        <Border Width="2"
                                                Height="16"
                                                Background="{StaticResource AccentBrush}"
                                                Margin="0,0,8,0"/>
                                        <TextBlock Text="LINES" Style="{StaticResource SectionHeaderTextStyle}"/>
                                    </StackPanel>

                                    <Grid Margin="0,0,0,18">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="160"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="50"/>
                                            <ColumnDefinition Width="42"/>
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <TextBlock Grid.Row="0"
                                                   Grid.Column="0"
                                                   Text="LINE COLOR"
                                                   Style="{StaticResource RowLabelTextStyle}"/>
                                        <Border x:Name="ProjectionLineColorPreview"
                                                Grid.Row="0"
                                                Grid.Column="1"
                                                Height="28"
                                                Background="{StaticResource FieldBrush}"
                                                BorderBrush="{StaticResource BorderBrushSoft}"
                                                BorderThickness="1"
                                                CornerRadius="3"
                                                Margin="0,0,8,0"
                                                ToolTip="By View"/>
                                        <Button Grid.Row="0"
                                                Grid.Column="2"
                                                Content="PICK"
                                                Tag="ProjectionLineColor"
                                                Style="{StaticResource SmallPrimaryButtonStyle}"
                                                Margin="0,0,6,0"
                                                Click="OnPickColor"/>
                                        <Button Grid.Row="0"
                                                Grid.Column="3"
                                                Content="BY"
                                                Tag="ProjectionLineColor"
                                                Style="{StaticResource SmallSecondaryButtonStyle}"
                                                Click="OnClearColor"/>
                                        <WrapPanel x:Name="ProjectionLineColorPresetPanel"
                                                   Grid.Row="1"
                                                   Grid.Column="1"
                                                   Grid.ColumnSpan="3"
                                                   Margin="0,8,0,0"/>
                                    </Grid>

                                    <Grid Margin="0,0,0,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="160"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="LINE PATTERN" Style="{StaticResource RowLabelTextStyle}"/>
                                        <ComboBox x:Name="ProjectionLinePatternCombo"
                                                  Grid.Column="1"
                                                  SelectionChanged="OnProjectionSurfaceSettingChanged"/>
                                    </Grid>

                                    <Grid Margin="0,0,0,20">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="160"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="LINE WEIGHT" Style="{StaticResource RowLabelTextStyle}"/>
                                        <ComboBox x:Name="ProjectionLineWeightCombo"
                                                  Grid.Column="1"
                                                  SelectionChanged="OnProjectionSurfaceSettingChanged"/>
                                    </Grid>

                                    <Border Height="1"
                                            Background="{StaticResource BorderBrushSoft}"
                                            Margin="0,0,0,18"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,14">
                                        <Border Width="2"
                                                Height="16"
                                                Background="{StaticResource AccentBrush}"
                                                Margin="0,0,8,0"/>
                                        <TextBlock Text="PATTERNS" Style="{StaticResource SectionHeaderTextStyle}"/>
                                    </StackPanel>

                                    <Grid Margin="0,0,0,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="160"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="FOREGROUND PATTERN" Style="{StaticResource RowLabelTextStyle}"/>
                                        <ComboBox x:Name="SurfaceForegroundPatternCombo"
                                                  Grid.Column="1"
                                                  SelectionChanged="OnProjectionSurfaceSettingChanged"/>
                                    </Grid>

                                    <Grid Margin="0,0,0,18">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="160"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="50"/>
                                            <ColumnDefinition Width="42"/>
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <TextBlock Grid.Row="0"
                                                   Grid.Column="0"
                                                   Text="FOREGROUND COLOR"
                                                   Style="{StaticResource RowLabelTextStyle}"/>
                                        <Border x:Name="SurfaceForegroundColorPreview"
                                                Grid.Row="0"
                                                Grid.Column="1"
                                                Height="28"
                                                Background="{StaticResource FieldBrush}"
                                                BorderBrush="{StaticResource BorderBrushSoft}"
                                                BorderThickness="1"
                                                CornerRadius="3"
                                                Margin="0,0,8,0"
                                                ToolTip="By View"/>
                                        <Button Grid.Row="0"
                                                Grid.Column="2"
                                                Content="PICK"
                                                Tag="SurfaceForegroundColor"
                                                Style="{StaticResource SmallPrimaryButtonStyle}"
                                                Margin="0,0,6,0"
                                                Click="OnPickColor"/>
                                        <Button Grid.Row="0"
                                                Grid.Column="3"
                                                Content="BY"
                                                Tag="SurfaceForegroundColor"
                                                Style="{StaticResource SmallSecondaryButtonStyle}"
                                                Click="OnClearColor"/>
                                        <WrapPanel x:Name="SurfaceForegroundColorPresetPanel"
                                                   Grid.Row="1"
                                                   Grid.Column="1"
                                                   Grid.ColumnSpan="3"
                                                   Margin="0,8,0,0"/>
                                    </Grid>

                                    <Grid Margin="0,0,0,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="160"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="BACKGROUND PATTERN" Style="{StaticResource RowLabelTextStyle}"/>
                                        <ComboBox x:Name="SurfaceBackgroundPatternCombo"
                                                  Grid.Column="1"
                                                  SelectionChanged="OnProjectionSurfaceSettingChanged"/>
                                    </Grid>

                                    <Grid Margin="0,0,0,18">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="160"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="50"/>
                                            <ColumnDefinition Width="42"/>
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <TextBlock Grid.Row="0"
                                                   Grid.Column="0"
                                                   Text="BACKGROUND COLOR"
                                                   Style="{StaticResource RowLabelTextStyle}"/>
                                        <Border x:Name="SurfaceBackgroundColorPreview"
                                                Grid.Row="0"
                                                Grid.Column="1"
                                                Height="28"
                                                Background="{StaticResource FieldBrush}"
                                                BorderBrush="{StaticResource BorderBrushSoft}"
                                                BorderThickness="1"
                                                CornerRadius="3"
                                                Margin="0,0,8,0"
                                                ToolTip="By View"/>
                                        <Button Grid.Row="0"
                                                Grid.Column="2"
                                                Content="PICK"
                                                Tag="SurfaceBackgroundColor"
                                                Style="{StaticResource SmallPrimaryButtonStyle}"
                                                Margin="0,0,6,0"
                                                Click="OnPickColor"/>
                                        <Button Grid.Row="0"
                                                Grid.Column="3"
                                                Content="BY"
                                                Tag="SurfaceBackgroundColor"
                                                Style="{StaticResource SmallSecondaryButtonStyle}"
                                                Click="OnClearColor"/>
                                        <WrapPanel x:Name="SurfaceBackgroundColorPresetPanel"
                                                   Grid.Row="1"
                                                   Grid.Column="1"
                                                   Grid.ColumnSpan="3"
                                                   Margin="0,8,0,0"/>
                                    </Grid>

                                    <Grid Margin="0,2,0,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="160"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="54"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0"
                                                   Text="TRANSPARENCY"
                                                   Style="{StaticResource RowLabelTextStyle}"/>
                                        <Slider x:Name="TransparencySlider"
                                                Grid.Column="1"
                                                Minimum="0"
                                                Maximum="100"
                                                TickFrequency="5"
                                                IsSnapToTickEnabled="True"
                                                VerticalAlignment="Center"
                                                ValueChanged="OnTransparencyChanged"/>
                                        <TextBlock x:Name="TransparencyValueText"
                                                   Grid.Column="2"
                                                   Text="0%"
                                                   Foreground="{StaticResource AccentBrush}"
                                                   FontSize="11"
                                                   FontWeight="Bold"
                                                   HorizontalAlignment="Right"
                                                   VerticalAlignment="Center"/>
                                    </Grid>
                                </StackPanel>
                            </TabItem>

                            <TabItem Header="CUT">
                                <StackPanel Margin="2,14,2,0" MinHeight="430">
                                    <Border Background="{StaticResource InnerPanelBrush}"
                                            BorderBrush="{StaticResource BorderBrushSoft}"
                                            BorderThickness="1"
                                            CornerRadius="4"
                                            Padding="12"
                                            Margin="0,0,0,18">
                                        <CheckBox x:Name="UseProjectionSurfaceColorsForCutCheckBox"
                                                  Content="USE PROJECTION SETTINGS FOR CUT"
                                                  Style="{StaticResource CutLinkCheckBoxStyle}"
                                                  Checked="OnUseProjectionSurfaceColorsForCutChanged"
                                                  Unchecked="OnUseProjectionSurfaceColorsForCutChanged"/>
                                    </Border>

                                    <TextBlock x:Name="CutLinkNoticeText"
                                               Text="CUT SETTINGS ARE CURRENTLY LINKED TO THE PROJECTION TAB."
                                               Foreground="{StaticResource TextDisabledBrush}"
                                               FontSize="11"
                                               FontStyle="Italic"
                                               HorizontalAlignment="Center"
                                               Margin="0,72,0,0"
                                               Visibility="Collapsed"/>

                                    <StackPanel x:Name="CutSettingsPanel">
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,14">
                                            <Border Width="2"
                                                    Height="16"
                                                    Background="{StaticResource AccentBrush}"
                                                    Margin="0,0,8,0"/>
                                            <TextBlock Text="LINES" Style="{StaticResource SectionHeaderTextStyle}"/>
                                        </StackPanel>

                                        <Grid Margin="0,0,0,18">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="160"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="50"/>
                                                <ColumnDefinition Width="42"/>
                                            </Grid.ColumnDefinitions>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>
                                            <TextBlock Grid.Row="0"
                                                       Grid.Column="0"
                                                       Text="CUT COLOR"
                                                       Style="{StaticResource RowLabelTextStyle}"/>
                                            <Border x:Name="CutLineColorPreview"
                                                    Grid.Row="0"
                                                    Grid.Column="1"
                                                    Height="28"
                                                    Background="{StaticResource FieldBrush}"
                                                    BorderBrush="{StaticResource BorderBrushSoft}"
                                                    BorderThickness="1"
                                                    CornerRadius="3"
                                                    Margin="0,0,8,0"
                                                    ToolTip="By View"/>
                                            <Button Grid.Row="0"
                                                    Grid.Column="2"
                                                    Content="PICK"
                                                    Tag="CutLineColor"
                                                    Style="{StaticResource SmallPrimaryButtonStyle}"
                                                    Margin="0,0,6,0"
                                                    Click="OnPickColor"/>
                                            <Button Grid.Row="0"
                                                    Grid.Column="3"
                                                    Content="BY"
                                                    Tag="CutLineColor"
                                                    Style="{StaticResource SmallSecondaryButtonStyle}"
                                                    Click="OnClearColor"/>
                                            <WrapPanel x:Name="CutLineColorPresetPanel"
                                                       Grid.Row="1"
                                                       Grid.Column="1"
                                                       Grid.ColumnSpan="3"
                                                       Margin="0,8,0,0"/>
                                        </Grid>

                                        <Grid Margin="0,0,0,8">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="160"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" Text="CUT PATTERN" Style="{StaticResource RowLabelTextStyle}"/>
                                            <ComboBox x:Name="CutLinePatternCombo" Grid.Column="1"/>
                                        </Grid>

                                        <Grid Margin="0,0,0,20">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="160"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" Text="CUT WEIGHT" Style="{StaticResource RowLabelTextStyle}"/>
                                            <ComboBox x:Name="CutLineWeightCombo" Grid.Column="1"/>
                                        </Grid>

                                        <Border Height="1"
                                                Background="{StaticResource BorderBrushSoft}"
                                                Margin="0,0,0,18"/>
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,14">
                                            <Border Width="2"
                                                    Height="16"
                                                    Background="{StaticResource AccentBrush}"
                                                    Margin="0,0,8,0"/>
                                            <TextBlock Text="PATTERNS" Style="{StaticResource SectionHeaderTextStyle}"/>
                                        </StackPanel>

                                        <Grid Margin="0,0,0,8">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="160"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" Text="FOREGROUND PATTERN" Style="{StaticResource RowLabelTextStyle}"/>
                                            <ComboBox x:Name="CutForegroundPatternCombo" Grid.Column="1"/>
                                        </Grid>

                                        <Grid Margin="0,0,0,18">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="160"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="50"/>
                                                <ColumnDefinition Width="42"/>
                                            </Grid.ColumnDefinitions>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>
                                            <TextBlock Grid.Row="0"
                                                       Grid.Column="0"
                                                       Text="FOREGROUND COLOR"
                                                       Style="{StaticResource RowLabelTextStyle}"/>
                                            <Border x:Name="CutForegroundColorPreview"
                                                    Grid.Row="0"
                                                    Grid.Column="1"
                                                    Height="28"
                                                    Background="{StaticResource FieldBrush}"
                                                    BorderBrush="{StaticResource BorderBrushSoft}"
                                                    BorderThickness="1"
                                                    CornerRadius="3"
                                                    Margin="0,0,8,0"
                                                    ToolTip="By View"/>
                                            <Button Grid.Row="0"
                                                    Grid.Column="2"
                                                    Content="PICK"
                                                    Tag="CutForegroundColor"
                                                    Style="{StaticResource SmallPrimaryButtonStyle}"
                                                    Margin="0,0,6,0"
                                                    Click="OnPickColor"/>
                                            <Button Grid.Row="0"
                                                    Grid.Column="3"
                                                    Content="BY"
                                                    Tag="CutForegroundColor"
                                                    Style="{StaticResource SmallSecondaryButtonStyle}"
                                                    Click="OnClearColor"/>
                                            <WrapPanel x:Name="CutForegroundColorPresetPanel"
                                                       Grid.Row="1"
                                                       Grid.Column="1"
                                                       Grid.ColumnSpan="3"
                                                       Margin="0,8,0,0"/>
                                        </Grid>

                                        <Grid Margin="0,0,0,8">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="160"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" Text="BACKGROUND PATTERN" Style="{StaticResource RowLabelTextStyle}"/>
                                            <ComboBox x:Name="CutBackgroundPatternCombo" Grid.Column="1"/>
                                        </Grid>

                                        <Grid Margin="0,0,0,18">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="160"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="50"/>
                                                <ColumnDefinition Width="42"/>
                                            </Grid.ColumnDefinitions>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>
                                            <TextBlock Grid.Row="0"
                                                       Grid.Column="0"
                                                       Text="BACKGROUND COLOR"
                                                       Style="{StaticResource RowLabelTextStyle}"/>
                                            <Border x:Name="CutBackgroundColorPreview"
                                                    Grid.Row="0"
                                                    Grid.Column="1"
                                                    Height="28"
                                                    Background="{StaticResource FieldBrush}"
                                                    BorderBrush="{StaticResource BorderBrushSoft}"
                                                    BorderThickness="1"
                                                    CornerRadius="3"
                                                    Margin="0,0,8,0"
                                                    ToolTip="By View"/>
                                            <Button Grid.Row="0"
                                                    Grid.Column="2"
                                                    Content="PICK"
                                                    Tag="CutBackgroundColor"
                                                    Style="{StaticResource SmallPrimaryButtonStyle}"
                                                    Margin="0,0,6,0"
                                                    Click="OnPickColor"/>
                                            <Button Grid.Row="0"
                                                    Grid.Column="3"
                                                    Content="BY"
                                                    Tag="CutBackgroundColor"
                                                    Style="{StaticResource SmallSecondaryButtonStyle}"
                                                    Click="OnClearColor"/>
                                            <WrapPanel x:Name="CutBackgroundColorPresetPanel"
                                                       Grid.Row="1"
                                                       Grid.Column="1"
                                                       Grid.ColumnSpan="3"
                                                       Margin="0,8,0,0"/>
                                        </Grid>
                                    </StackPanel>
                                </StackPanel>
                            </TabItem>

                        </TabControl>

                        <Border BorderBrush="{StaticResource BorderBrushSoft}"
                                BorderThickness="0,1,0,0"
                                Margin="0,12,0,0"
                                Padding="0,10,0,0">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="ENABLE HALFTONE OVERRIDE"
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           FontSize="11"
                                           FontWeight="Bold"
                                           VerticalAlignment="Center"/>
                                <CheckBox x:Name="HalftoneCheckBox"
                                          Grid.Column="1"
                                          Style="{StaticResource ToggleSwitchStyle}"/>
                            </Grid>
                        </Border>

                        <TextBlock x:Name="ErrorText"
                                   Foreground="#FFFF7C7C"
                                   FontSize="11"
                                   FontWeight="SemiBold"
                                   TextWrapping="Wrap"
                                   Margin="0,12,0,0"/>
                    </StackPanel>
                </Border>
            </ScrollViewer>

            <Border Grid.Row="1"
                    Background="{StaticResource ShellBrush}"
                    BorderBrush="{StaticResource BorderBrushSoft}"
                    BorderThickness="0,1,0,0"
                    Padding="14,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0"
                            Content="RESET"
                            Style="{StaticResource FooterSecondaryButtonStyle}"
                            Click="OnReset"/>
                    <StackPanel Grid.Column="2"
                                Orientation="Horizontal">
                        <Button x:Name="ApplyElementsButton"
                                Content="ELEMENTS"
                                Style="{StaticResource FooterPrimaryButtonStyle}"
                                IsDefault="True"
                                Margin="0,0,8,0"
                                Click="OnApplyElements"/>
                        <Button x:Name="ApplyCategoriesButton"
                                Content="CATEGORIES"
                                Style="{StaticResource FooterPrimaryButtonStyle}"
                                Margin="0,0,8,0"
                                Click="OnApplyCategories"/>
                        <Button Content="CANCEL"
                                Style="{StaticResource FooterDangerButtonStyle}"
                                IsCancel="True"
                                Click="OnCancel"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
