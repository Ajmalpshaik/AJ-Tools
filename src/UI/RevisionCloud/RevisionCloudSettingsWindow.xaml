<Window x:Class="AJTools.UI.RevisionCloud.RevisionCloudSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Revision Cloud By Elements"
        Width="400" Height="250"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#FF1E1E1E">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../ModernStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="Revision Cloud By Elements"
                   Foreground="{StaticResource TextPrimary}"
                   FontSize="16" FontWeight="Bold" Margin="0,0,0,12"/>

        <!-- Cloud Settings -->
        <GroupBox Grid.Row="1" Header="Cloud Settings" Style="{StaticResource ModernGroupBox}">
            <Grid Margin="0,4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="180"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="30"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Offset Distance" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center"/>
                <TextBox x:Name="txtOffset" Grid.Column="1" Style="{StaticResource ModernTextBox}" Text="50"/>
                <TextBlock Grid.Column="2" Text="mm" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center" Margin="4,0,0,0"/>
            </Grid>
        </GroupBox>

        <!-- Info -->
        <TextBlock Grid.Row="2" Text="Used by Cloud By Elements > Place. Press Esc while selecting to stop continuous run."
                   Foreground="{StaticResource TextSecondary}" FontSize="10" Margin="5,8,0,0" FontStyle="Italic"
                   VerticalAlignment="Top"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="Save" Style="{StaticResource ModernButton}" Width="100"
                    Click="BtnCreate_Click" Margin="0,0,8,0"/>
            <Button Content="Cancel" Style="{StaticResource SecondaryButton}" Width="100"
                    Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
